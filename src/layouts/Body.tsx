import Chat from '../components/chat/Chat';

const Body = () => {
    return (
        <div className="flex justify-center h-screen bg-gray-50 pt-16">
            <Chat/>
        </div>
    );
};

export default Body;
